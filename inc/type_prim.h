#ifndef TYPE_PRIM_H
#define TYPE_PRIM_H

#include "type.h"

type_data(Char, char);
type_data(Int, int);
type_data(Long, long);
type_data(LongDouble, long double);
type_data(Float, float);
type_data(Double, double);
type_data(UnsignedChar, unsigned char);
type_data(Unsigned, unsigned);
type_data(UnsignedLong, unsigned long);

#endif
